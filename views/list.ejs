<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>To do list</title>
  </head>
  <body>

    <% if (kindOfDay === "Saturday" || "Sunday"){ %>
    <h1 style="color:purple">It's a <%= kindOfDay %>!</h1>
  <% } else { %>
    <h1 style="color:green">It's a <%= kindOfDay %>!</h1>
  <% } %>
  </body>
</html>
